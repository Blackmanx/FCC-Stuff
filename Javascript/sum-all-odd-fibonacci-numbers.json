{"index.js":"function sumFibs(num) {\nlet sum = 0;\nlet previousNum = 0, nextNum = 1;\n// Starts at 1.\n// PreviousNum = 0, NextNum = 1\n// Next should be NextNum = PreviousNum +NextNum, then Nextnum = PreviousNextNum. It kinda works like a Dynamic list.\nif (num > 1){\nfor (let i = 1; i <= num; i = nextNum){ // Next number to check is nextNum.\n  if (i % 2 != 0){ // If odds, skip this step. If not, sum them.\n    sum += i;\n  }\n// Store current nextNum\nlet temp = nextNum;\n// NextNum becomes the sum of itself and previousNum\nnextNum += previousNum;\n// previousNum becomes the previous nextNum, stored in temp.\npreviousNum= temp;\n\nconsole.log(previousNum);\nconsole.log(nextNum);\n}\n} else return 1;\nconsole.log(sum);\n  return sum;\n}\n\nsumFibs(4);"}