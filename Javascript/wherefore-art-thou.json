{"index.js":"function whatIsInAName(collection, source) {\n  // What's in a name?\n  // Only change code below this line\n\n  let sourceKeys = Object.keys(source);\n  console.log(Object.keys(source));\n  return collection.filter( (item) => {\n    return sourceKeys.every( (key) => {\n      console.log(item[key]);\n      return item.hasOwnProperty(key) && item[key] == source[key];\n    });\n  });\n  \n/** get the keys from source that you need to identify (getting last in this case)\n *  filter every item inside the collection array, with a function that identifies the key (last), and also identifies which object has the key with an item that equals the source key's item.\n * \n */\n\n  // Only change code above this line\n}\n\nwhatIsInAName([{ first: \"Romeo\", last: \"Montague\" }, { first: \"Mercutio\", last: null }, { first: \"Tybalt\", last: \"Capulet\" }], { last: \"Capulet\" });"}